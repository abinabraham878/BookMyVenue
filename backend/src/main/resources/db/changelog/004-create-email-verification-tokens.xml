<?xml version="1.0" encoding="UTF-8"?>

<databaseChangeLog
        xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
        xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
        xsi:schemaLocation="
            http://www.liquibase.org/xml/ns/dbchangelog
            https://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-latest.xsd">

    <changeSet id="004-create-email-verification-tokens" author="abin">

        <preConditions onFail="MARK_RAN">
            <not>
                <tableExists tableName="email_verification_tokens"/>
            </not>
        </preConditions>

        <createTable tableName="email_verification_tokens">

            <column name="id" type="UUID">
                <constraints
                        primaryKey="true"
                        primaryKeyName="pk_email_verification_tokens"
                        nullable="false"/>
            </column>

            <column name="user_id" type="UUID">
                <constraints nullable="false"/>
            </column>

            <column name="token_hash" type="VARCHAR(255)">
                <constraints nullable="false"/>
            </column>

            <column name="expires_at" type="TIMESTAMPTZ">
                <constraints nullable="false"/>
            </column>

            <column name="verified_at" type="TIMESTAMPTZ"/>

            <column name="created_at"
                    type="TIMESTAMPTZ"
                    defaultValueComputed="CURRENT_TIMESTAMP">
                <constraints nullable="false"/>
            </column>

        </createTable>

        <addForeignKeyConstraint
                baseTableName="email_verification_tokens"
                baseColumnNames="user_id"
                referencedTableName="users"
                referencedColumnNames="id"
                constraintName="fk_email_verification_tokens_user_id"/>

        <addUniqueConstraint
                tableName="email_verification_tokens"
                columnNames="user_id"
                constraintName="uk_email_verification_tokens_user_id"/>

        <createIndex
                tableName="email_verification_tokens"
                indexName="idx_email_verification_tokens_expires_at">
            <column name="expires_at"/>
        </createIndex>

        <rollback>
            <dropTable tableName="email_verification_tokens"/>
        </rollback>

    </changeSet>

</databaseChangeLog>